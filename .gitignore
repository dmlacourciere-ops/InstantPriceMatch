# Ignore EVERYTHING by default
*

# Allow folders so we can descend into them
!*/

# Allow only source / text files
!*.py
!*.md
!*.json
!*.ps1
!*.bat
!requirements.txt
!.gitignore
!.gitattributes

# Allow chat logs in repo
!docs/chat-logs/
!docs/chat-logs/*.txt

# Allow chat logs in repo
!docs/chat-logs/
!docs/chat-logs/*.txt

# Allow chat logs in repo
!docs/chat-logs/
!docs/chat-logs/*.txt

# Allow chat logs in repo
!docs/chat-logs/
!docs/chat-logs/*.txt

.env

